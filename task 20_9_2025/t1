1. الفرق بين الـ Class والـ Object

الـ Class (الكلاس): ده مجرد تصميم أو مخطط، بيقول الشكل والخصائص والسلوك هيكونوا إزاي.

الـ Object (الأوبجكت): ده النسخة الحقيقية اللي اتعملت من التصميم ده.

💡 مثال من الحياة الواقعية:

الـ Class = تصميم عربية (بيقول فيها 4 كاوتش – لون – موتور).

الـ Object = العربية بتاعتك اللي واقفة في الجراج (تويوتا كورولا لونها أحمر).

يعني: Class = فكرة/خطة | Object = الشيء الحقيقي.

2. يعني إيه Constructor؟

الـ Constructor (الكونستركتور) هو دالة خاصة بتشتغل أوتوماتيك أول ما تعمل Object جديد من الكلاس. وظيفته يجهز القيم ويبتدي الأوبجكت.

 أنواع الـ Constructor في Dart:

Default constructor → لو ما كتبتش، دارت بيعمل واحد فاضي تلقائي.

Parameterized constructor → تسمحلك تبعت قيم وانت بتعمل object.

Named constructor → كونستركتور باسمه مخصوص عشان تعمل object بأكتر من طريقة.

3. الـ Access Modifiers في Dart

في Java عندك public / private / protected.
لكن في Dart الموضوع أبسط:

أي حاجة تبدأ بـ Underscore _ → تبقى Private (مش هتقدر توصلها غير من نفس الملف).

لو مفيش Underscore → تبقى Public (ممكن تستخدمها في أي مكان).

 مفيش حاجة اسمها protected زي Java.

4. يعني إيه Encapsulation ?

الكبسلة معناها إنك تخفي التفاصيل الداخلية بتاعت الكلاس وتسمح بالوصول ليها بطريقة محمية ومتحكم فيها.

في Dart بنستخدم Getter و Setter:

class BankAccount {
  double _balance = 0;   // متغير خاص (Private)

  // Getter (عشان أرجع الرصيد)
  double get balance => _balance;

  // Setter (عشان أودع فلوس)
  set deposit(double amount) {
    if (amount > 0) {
      _balance += amount;
    }
  }
}


هنا الناس ما يقدروش يغيروا _balance على طول، لازم يعدّوا من خلال الـ setter.

5. ليه نستخدم Setter بدل ما نخلي المتغير Public؟

لو خليت المتغير Public → أي حد ممكن يغيره زي ما هو عايز (ممكن يحط -100 في الرصيد).

لكن بالـ Setter → تقدر تضيف قواعد وفحص قبل ما تتغير القيمة.

💡 مثال:

account.deposit = -100; // ❌ مش هتشتغل لأن فيه شرط في الـ Setter


ده بيخلي بياناتك محمية وآمنة.